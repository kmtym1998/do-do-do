editTask(
    '<%= "#{ @taskTitle }" %>',
    '<%= "#{ @deadline }" %>',
    '<%= "#{ @priority }" %>',
    '<%= "#{ @state }" %>',
    '<%= "#{ @memo }" %>'
);

function editTask(title, deadline, priority, state, memo){
    
}